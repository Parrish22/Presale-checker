services:
  - type: worker
    name: presale-tracker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python presale_tracker.py
    envVars:
      - key: DISCORD_PRESALE_WEBHOOK_URL
        sync: false
